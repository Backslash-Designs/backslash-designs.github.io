name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm install --no-audit --no-fund
      # Load secrets from 1Password and export as environment variables for subsequent steps
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v3
        with:
          export-env: true
        env:
          # This GitHub secret must contain your 1Password Service Account token
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          # Map the env var name to the 1Password item reference. Update the vault/item/field path.
          BLOGS_GH_TOKEN: op://Project-Origin/project-backlslash-readonly/credential
      # NEW: prefetch blogs.json from the private repo (uses token server-side)
      - name: Prefetch blogs.json (private repo)
        run: |
          mkdir -p src/pages/blog
          echo "Fetching ${VITE_BLOGS_OWNER}/${VITE_BLOGS_REPO}:${VITE_BLOGS_PATH}@${VITE_BLOGS_REF}"
          curl -fsSL \
            -H "Authorization: Bearer ${BLOGS_GH_TOKEN}" \
            -H "Accept: application/vnd.github.raw" \
            "https://api.github.com/repos/${VITE_BLOGS_OWNER}/${VITE_BLOGS_REPO}/contents/${VITE_BLOGS_PATH}?ref=${VITE_BLOGS_REF}" \
            -o src/pages/blog/blogs.json \
          || echo "Prefetch failed; using bundled src/pages/blog/blogs.json"
        env:
          VITE_BLOGS_OWNER: backslash-designs
          VITE_BLOGS_REPO: Project-Backslash-Chronicle
          VITE_BLOGS_PATH: index.json
          VITE_BLOGS_REF: master
      - run: npm run build
        env:
          VITE_BLOGS_OWNER: backslash-designs
          VITE_BLOGS_REPO: Project-Backslash-Chronicle
          VITE_BLOGS_PATH: index.json
          VITE_BLOGS_REF: master
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
